import ExploreMap from "../components/ExploreMap";

export const metadata = {
  title: "Kaart — MAG IK HIER WILDPLASSEN?",
  description: "Verken de kaart en check per plek of je er mag wildplassen.",
};

export default function KaartPage() {
  return <ExploreMap />;
}
